SwiftCart Sitemap - Buttons & Destinations Only

Entry Points:
├── login.html
│   ├── "Sign In as User" → index.html
│   └── "Sign In as Admin" → admin.html

Main Navigation (all pages except login):
├── "Catalog" → index.html
├── "Cart" (🛒 icon) → cart.html
├── "Checkout" → checkout.html
├── "Status" → status.html
└── "Dashboard" (admin only) → admin.html
└── "Logout" → login.html

Shopping Flow:
├── index.html (Catalog)
│   └── "Add to Cart" (per product) → cart.html
├── cart.html
│   ├── "+" / "-" → update cart.html
│   ├── "Remove" → update cart.html
│   └── "Proceed to Checkout" → checkout.html
├── checkout.html
│   └── "Place Order" → status.html
└── status.html (no buttons, view-only)

Admin Only (admin.html):
└── "Add Product" → update admin.html (mock)


